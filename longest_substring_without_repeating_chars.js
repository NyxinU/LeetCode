const lengthOfLongestSubstring = (s) => {
  const maxLength = 0;
  let temp = [];
  const arrOfletters = s.split('');

  let i = 0;
  for (let j = (i + 1); j < arrOfletters.length; j++) {
    if(temp.includes())
  };

const isUniq = (s) => {

};